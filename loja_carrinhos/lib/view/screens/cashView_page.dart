import 'package:flutter/material.dart';

class ViewCashPage extends StatelessWidget {
  const ViewCashPage({ Key key });

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}